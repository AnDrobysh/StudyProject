package Lesson_2.exercises;

/*Исправьте код, чтобы он корректно определял количество дней в заданном месяце, учитывая, что 2 — это февраль.*/
/*ПЕРЕД ТЕМ КАК ВЫПОЛНЯТЬ ЗАДАНИЕ УДАЛИТЕ *//* и *//* В НАЧАЛЕ И КОНЦЕ БЛОКА КОДА, с текста эти символы удалять НЕ НУЖНО*/


/*
public class Exercise2 {
    public static void main(String[] args) {
        int month = 2;
        int year = 2024; // Високосный год
        int days;

        switch (month) {
            case 1: case 3: case 5: case 7: case 8: case 10: case 12:
                days = 31;
                break;
            case 4: case 6: case 9: case 11:
                days = 30;
                break;
            case 2:
                if ((year % 4 == 0) && (year % 100 != 0) && (year % 400 == 0))
                    days = 29;
        } else {
            days = 28;
        }
        break;
        default:
        days = 0;
    }
        System.out.println("Количество дней: " + days);
}
}
*/

