package Lesson_2.exercises;

/*Исправьте код так, чтобы он корректно находил сумму всех элементов массива, но пропускал отрицательные числа.*/
/*ПЕРЕД ТЕМ КАК ВЫПОЛНЯТЬ ЗАДАНИЕ УДАЛИТЕ *//* и *//* В НАЧАЛЕ И КОНЦЕ БЛОКА КОДА, с текста эти символы удалять НЕ НУЖНО*/

/*
public class Exercise6 {
    public static void main(String[] args) {
        int[] numbers = {1, -2, 3, -4, 5};
        int sum = 0;
        for (int num : numbers) {
            if (num < 0);
            continue;
            sum += num;
        }
        System.out.println("Сумма положительных чисел: " + sum);
    }
}
*/
